name: CD – QA (Firebase App Distribution + TestFlight)

on:
  push:
    branches: [Onz_Android]

concurrency:
  group: cd-qa
  cancel-in-progress: true

# ────────────────────────────────────────────────
# 필요한 GitHub Secrets
# ────────────────────────────────────────────────
# [Android]
#   ANDROID_KEYSTORE_BASE64         - base64로 인코딩한 .jks 파일
#                                     (macOS/Linux) base64 -i android/app/my-release-key.jks | pbcopy
#   ANDROID_STORE_PASSWORD          - 키스토어 비밀번호
#   ANDROID_KEY_ALIAS               - 키 별칭 (my-key-alias)
#   ANDROID_KEY_PASSWORD            - 키 비밀번호
#   FIREBASE_TOKEN                  - firebase login:ci 로 발급
#   FIREBASE_ANDROID_APP_ID         - 1:513269758316:android:8f3b11afa11de306391b49
#   FIREBASE_IOS_APP_ID             - 1:513269758316:ios:66fdd942934a6d90391b49
#                                     (없으면 iOS Firebase 배포만 건너뛴다)
#
# [iOS]
#   IOS_DIST_CERT_P12_BASE64        - 배포용 .p12 인증서 base64
#   IOS_DIST_CERT_PASSWORD          - .p12 비밀번호
#   IOS_APP_STORE_PROFILE_BASE64    - App Store 배포 프로비저닝 프로파일 base64
#   IOS_ADHOC_PROFILE_BASE64        - Ad Hoc 프로비저닝 프로파일 base64 (선택)
#                                     Firebase 배포용. 테스터 기기 UDID 가 등록된
#                                     프로파일이어야 하고, 기기가 늘 때마다 재발급·갱신.
#                                     base64 -i AdHoc.mobileprovision | pbcopy
#   APPLE_TEAM_ID                   - Apple Developer Team ID
#   APP_STORE_CONNECT_API_KEY_ID    - ASC API Key ID
#   APP_STORE_CONNECT_API_ISSUER_ID - ASC API Issuer ID
#   APP_STORE_CONNECT_API_KEY_CONTENT - .p8 파일 내용 (-----BEGIN PRIVATE KEY----- 포함)
#
# [공통]
#   SENTRY_AUTH_TOKEN              - sourcemap/dSYM/ProGuard 매핑 업로드용
#                                   (sentry.io → Settings → Auth Tokens, project:releases 스코프)
# ────────────────────────────────────────────────

jobs:
  # ──────────────────────────────────────────────
  # Android → Firebase App Distribution
  # ──────────────────────────────────────────────
  android-firebase:
    name: Android → Firebase App Distribution
    runs-on: ubuntu-latest

    env:
      ANDROID_KEYSTORE_PATH: my-release-key.jks
      ANDROID_STORE_PASSWORD: ${{ secrets.ANDROID_STORE_PASSWORD }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install JS dependencies
        run: npm install --ignore-scripts

      - name: Apply patches
        run: npx patch-package

      - name: Create .env file
        run: |
          # QA 내부배포: 로컬 BE(신규 바/뉴스/채팅/매거진 포함)를 Tailscale Funnel 로 노출한 고정 URL.
          # cloudflared quick 터널과 달리 재발급되지 않으므로 APK 를 다시 굽지 않아도 계속 유효하다.
          # 프로덕션(onz-cocktail.kr)에는 아직 신규 기능이 없어 QA 만 이 주소를 바라본다.
          echo "API_BASE_URL=http://onz-cocktail.kr/onz" >> .env
          echo "GOOGLE_PLACES_API_KEY=AIzaSyDeWHr3QIavBa4RGYwZly8h0sNTfLmQ" >> .env
          echo "GOOGLE_CLIENT_ID=1058340377075-vt8u6qabph0f0van79eqhkt9j2f1jkbe.apps.googleusercontent.com" >> .env
          echo "GOOGLE_REDIRECT_URI=com.googleusercontent.apps.1058340377075-vt8u6qabph0f0van79eqhkt9j2f1jkbe:/oauth2redirect/google" >> .env
          echo "SENTRY_ENV=qa" >> .env

      - name: Create sentry.properties
        run: |
          # sentry.gradle / sentry-xcode 스크립트가 sourcemap·ProGuard 매핑을 업로드하려면 필요.
          # 없으면 업로드가 조용히 스킵돼 프로덕션 스택이 난독화된 채로 온다.
          for d in android ios; do
            printf 'auth.token=%s\ndefaults.org=onz-yt\ndefaults.project=react-native\ndefaults.url=https://sentry.io/\n' \
              "${{ secrets.SENTRY_AUTH_TOKEN }}" > "$d/sentry.properties"
          done

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      # 러너 프리인스톨 버전(28.2)을 여기 박아 두고 있었는데, build.gradle 은
      # vision-camera 링크 크래시 때문에 27.1 로 되돌아갔다(21974d71). 둘이 어긋나
      # [CXX1104] 로 APK 빌드가 깨졌다 → build.gradle 을 단일 출처로 삼고, 러너에
      # 없는 버전이면 설치한다.
      - name: Configure NDK (build.gradle 의 ndkVersion 을 따른다)
        run: |
          NDK_VERSION=$(grep -o 'ndkVersion = "[^"]*"' android/build.gradle | head -1 | cut -d'"' -f2)
          if [ -z "$NDK_VERSION" ]; then
            echo "build.gradle 에서 ndkVersion 을 찾지 못했다." >&2
            exit 1
          fi
          echo "ndkVersion=$NDK_VERSION"
          if [ ! -d "${ANDROID_HOME}/ndk/${NDK_VERSION}" ]; then
            echo "러너에 없다 → sdkmanager 로 설치."
            "${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager" --install "ndk;${NDK_VERSION}"
          fi
          printf 'sdk.dir=%s\nndk.dir=%s/ndk/%s\n' \
            "${ANDROID_HOME}" "${ANDROID_HOME}" "${NDK_VERSION}" > android/local.properties

      - name: Decode keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | tr -d ' \r\n' | base64 -d > android/app/my-release-key.jks

      - name: Build Release APK
        env:
          VERSION_CODE: ${{ github.run_number }}
        run: cd android && ./gradlew assembleRelease --no-daemon --no-configuration-cache

      - name: Upload to Firebase App Distribution
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_ANDROID_APP_ID }}
          token: ${{ secrets.FIREBASE_TOKEN }}
          groups: testers
          file: android/app/build/outputs/apk/release/app-release.apk
          releaseNotes: |
            Branch: ${{ github.ref_name }}
            Commit: ${{ github.sha }}
            ${{ github.event.head_commit.message }}

  # ──────────────────────────────────────────────
  # iOS → TestFlight
  # ──────────────────────────────────────────────
  ios-testflight:
    name: iOS → TestFlight (+ Firebase, 시크릿 있을 때만)
    runs-on: macos-latest

    env:
      NODE_OPTIONS: "--max-old-space-size=16384"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install JS dependencies
        run: npm install --ignore-scripts

      - name: Apply patches
        run: npx patch-package

      - name: Create .env file
        run: |
          # Android 잡과 동일한 QA 주소를 바라본다. Tailscale Funnel 고정 주소라 자동 갱신이 필요 없다.
          echo "API_BASE_URL=http://onz-cocktail.kr/onz" >> .env
          echo "GOOGLE_PLACES_API_KEY=AIzaSyDeWHr3QIavBa4RGYwZly8h0sNTfLmQ" >> .env
          echo "GOOGLE_CLIENT_ID=1058340377075-vt8u6qabph0f0van79eqhkt9j2f1jkbe.apps.googleusercontent.com" >> .env
          echo "GOOGLE_REDIRECT_URI=com.googleusercontent.apps.1058340377075-vt8u6qabph0f0van79eqhkt9j2f1jkbe:/oauth2redirect/google" >> .env
          echo "SENTRY_ENV=qa" >> .env

      - name: Create sentry.properties
        run: |
          # sentry-xcode 스크립트가 sourcemap·dSYM 을 업로드하려면 필요.
          # 없으면 업로드가 조용히 스킵돼 프로덕션 스택이 난독화된 채로 온다.
          for d in android ios; do
            printf 'auth.token=%s\ndefaults.org=onz-yt\ndefaults.project=react-native\ndefaults.url=https://sentry.io/\n' \
              "${{ secrets.SENTRY_AUTH_TOKEN }}" > "$d/sentry.properties"
          done

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler: latest
          bundler-cache: true

      - name: Install CocoaPods
        run: bundle exec pod install --no-repo-update --project-directory=ios

      - name: Import distribution certificate
        env:
          CERT_BASE64: ${{ secrets.IOS_DIST_CERT_P12_BASE64 }}
        run: |
          CERT_PATH=$RUNNER_TEMP/dist.p12
          KEYCHAIN_PATH=$RUNNER_TEMP/ios-build.keychain
          KEYCHAIN_PASS=$(openssl rand -base64 20)

          echo "$CERT_BASE64" | python3 -c "import base64,sys; sys.stdout.buffer.write(base64.b64decode(sys.stdin.read().replace('\n','').replace('\r','').replace(' ','')))" > $CERT_PATH

          security create-keychain -p "$KEYCHAIN_PASS" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASS" $KEYCHAIN_PATH
          security import $CERT_PATH \
            -P "${{ secrets.IOS_DIST_CERT_PASSWORD }}" \
            -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH login.keychain-db
          security default-keychain -s $KEYCHAIN_PATH

          echo "KEYCHAIN_PATH=$KEYCHAIN_PATH" >> $GITHUB_ENV
          echo "KEYCHAIN_PASS=$KEYCHAIN_PASS" >> $GITHUB_ENV

      - name: Import provisioning profile
        env:
          PROFILE_BASE64: ${{ secrets.IOS_APP_STORE_PROFILE_BASE64 }}
        run: |
          PP_PATH=$RUNNER_TEMP/profile.mobileprovision
          echo "$PROFILE_BASE64" | python3 -c "import base64,sys; sys.stdout.buffer.write(base64.b64decode(sys.stdin.read().replace('\n','').replace('\r','').replace(' ','')))" > $PP_PATH

          PP_UUID=$(/usr/libexec/PlistBuddy -c "Print :UUID" \
            /dev/stdin <<< $(security cms -D -i $PP_PATH))

          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles/$PP_UUID.mobileprovision

          echo "PROVISIONING_PROFILE_UUID=$PP_UUID" >> $GITHUB_ENV

      - name: Configure Xcode signing (main target only)
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          echo "=== Signing identities in keychain ==="
          security find-identity -v -p codesigning
          echo "=== Provisioning profile UUID ==="
          echo "$PROVISIONING_PROFILE_UUID"
          cd ios
          bundle exec ruby << 'RUBY'
          require 'xcodeproj'
          project = Xcodeproj::Project.open('Cocktail_Front.xcodeproj')
          target = project.targets.find { |t| t.name == 'Cocktail_Front' }
          config = target.build_configurations.find { |c| c.name == 'Release' }
          config.build_settings['CODE_SIGN_STYLE'] = 'Manual'
          config.build_settings['PROVISIONING_PROFILE'] = ENV['PROVISIONING_PROFILE_UUID']
          config.build_settings['PROVISIONING_PROFILE_SPECIFIER'] = ENV['PROVISIONING_PROFILE_UUID']
          config.build_settings['CODE_SIGN_IDENTITY'] = 'iPhone Distribution'
          config.build_settings['DEVELOPMENT_TEAM'] = ENV['APPLE_TEAM_ID']
          project.save
          puts "Signing configured: target=#{target.name}, profile=#{ENV['PROVISIONING_PROFILE_UUID']}, team=#{ENV['APPLE_TEAM_ID']}"
          RUBY

      - name: Set build number
        run: |
          cd ios
          agvtool new-version -all $GITHUB_RUN_NUMBER

      - name: Pre-bundle JS (validate before archive)
        run: |
          npx react-native bundle \
            --platform ios \
            --dev false \
            --entry-file index.js \
            --bundle-output /tmp/ios-main.jsbundle \
            --assets-dest /tmp/ios-assets

      - name: Clean DerivedData
        run: rm -rf ~/Library/Developer/Xcode/DerivedData

      - name: Archive
        run: |
          xcodebuild archive \
            -workspace ios/Cocktail_Front.xcworkspace \
            -scheme Cocktail_Front \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath $RUNNER_TEMP/Cocktail_Front.xcarchive

      - name: Export IPA (app-store)
        run: |
          cat > $RUNNER_TEMP/ExportOptions.plist << PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>method</key><string>app-store-connect</string>
            <key>teamID</key><string>${{ secrets.APPLE_TEAM_ID }}</string>
            <key>signingStyle</key><string>manual</string>
            <key>signingCertificate</key><string>iPhone Distribution</string>
            <key>provisioningProfiles</key><dict>
              <key>org.reactjs.native.example.Cocktail-Front</key><string>${PROVISIONING_PROFILE_UUID}</string>
            </dict>
          </dict></plist>
          PLIST

          xcodebuild -exportArchive \
            -archivePath $RUNNER_TEMP/Cocktail_Front.xcarchive \
            -exportOptionsPlist $RUNNER_TEMP/ExportOptions.plist \
            -exportPath $RUNNER_TEMP/ipa

      - name: Upload to TestFlight
        env:
          API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}
          API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
        run: |
          mkdir -p ~/.appstoreconnect/private_keys
          echo "$API_KEY_CONTENT" | python3 -c "import base64,sys; sys.stdout.buffer.write(base64.b64decode(sys.stdin.read().replace('\n','').replace('\r','').replace(' ','')))" > ~/.appstoreconnect/private_keys/AuthKey_${API_KEY_ID}.p8

          xcrun altool --upload-app \
            --type ios \
            --file $(find $RUNNER_TEMP/ipa -name "*.ipa" | head -1) \
            --apiKey "$API_KEY_ID" \
            --apiIssuer "$API_ISSUER_ID"

      # ── Firebase App Distribution (선택) ────────────────────────────
      # TestFlight 는 테스터를 App Store Connect 팀에 넣어야 해서, 팀 계정을
      # 줄 수 없는 사람에게는 못 돌린다. 그런 테스터용으로 같은 아카이브를
      # ad-hoc 으로 한 번 더 export 해서 Firebase 로 보낸다.
      #
      # ad-hoc 은 기기 UDID 가 프로파일에 박혀 있어야만 설치된다. 테스터가
      # 늘면 Apple Developer 포털에서 UDID 를 추가하고 프로파일을 재발급해
      # IOS_ADHOC_PROFILE_BASE64 를 갱신해야 한다. 그 품이 싫으면 TestFlight 다.
      #
      # 두 시크릿이 없으면 통째로 건너뛴다. TestFlight 경로는 영향받지 않는다.
      - name: Check Firebase iOS secrets
        id: fb_ios
        env:
          ADHOC_PROFILE: ${{ secrets.IOS_ADHOC_PROFILE_BASE64 }}
          FIREBASE_IOS_APP_ID: ${{ secrets.FIREBASE_IOS_APP_ID }}
        run: |
          if [ -n "$ADHOC_PROFILE" ] && [ -n "$FIREBASE_IOS_APP_ID" ]; then
            echo "ready=true" >> $GITHUB_OUTPUT
          else
            echo "ready=false" >> $GITHUB_OUTPUT
            echo "::notice::IOS_ADHOC_PROFILE_BASE64 또는 FIREBASE_IOS_APP_ID 가 없어 Firebase iOS 배포를 건너뜁니다."
          fi

      - name: Import ad-hoc provisioning profile
        if: steps.fb_ios.outputs.ready == 'true'
        env:
          PROFILE_BASE64: ${{ secrets.IOS_ADHOC_PROFILE_BASE64 }}
        run: |
          PP_PATH=$RUNNER_TEMP/adhoc.mobileprovision
          echo "$PROFILE_BASE64" | python3 -c "import base64,sys; sys.stdout.buffer.write(base64.b64decode(sys.stdin.read().replace('\n','').replace('\r','').replace(' ','')))" > $PP_PATH

          PP_UUID=$(/usr/libexec/PlistBuddy -c "Print :UUID" \
            /dev/stdin <<< $(security cms -D -i $PP_PATH))

          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles/$PP_UUID.mobileprovision

          # 프로파일에 박힌 기기 수를 남겨둔다. "설치가 안 된다"는 문의는
          # 십중팔구 그 기기 UDID 가 여기 없는 경우라, 로그만 보고 판별된다.
          security cms -D -i $PP_PATH > $RUNNER_TEMP/adhoc.plist
          DEVICE_COUNT=$(/usr/libexec/PlistBuddy -c "Print :ProvisionedDevices" $RUNNER_TEMP/adhoc.plist 2>/dev/null | grep -cE '^\s{4}[0-9a-fA-F]' || true)
          echo "ad-hoc 프로파일 UUID=$PP_UUID / 등록된 기기 ${DEVICE_COUNT:-0}대"

          echo "ADHOC_PROFILE_UUID=$PP_UUID" >> $GITHUB_ENV

      # 아카이브를 다시 만들지 않는다. exportArchive 가 지정한 프로파일로 다시
      # 서명하므로, TestFlight 용으로 만든 .xcarchive 를 그대로 재사용한다.
      # (여기서 아카이브를 또 뜨면 잡 시간이 두 배가 된다)
      - name: Export IPA (ad-hoc)
        if: steps.fb_ios.outputs.ready == 'true'
        run: |
          cat > $RUNNER_TEMP/ExportOptionsAdHoc.plist << PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>method</key><string>ad-hoc</string>
            <key>teamID</key><string>${{ secrets.APPLE_TEAM_ID }}</string>
            <key>signingStyle</key><string>manual</string>
            <key>signingCertificate</key><string>iPhone Distribution</string>
            <key>provisioningProfiles</key><dict>
              <key>org.reactjs.native.example.Cocktail-Front</key><string>${ADHOC_PROFILE_UUID}</string>
            </dict>
          </dict></plist>
          PLIST

          xcodebuild -exportArchive \
            -archivePath $RUNNER_TEMP/Cocktail_Front.xcarchive \
            -exportOptionsPlist $RUNNER_TEMP/ExportOptionsAdHoc.plist \
            -exportPath $RUNNER_TEMP/ipa-adhoc

          echo "ADHOC_IPA=$(find $RUNNER_TEMP/ipa-adhoc -name '*.ipa' | head -1)" >> $GITHUB_ENV

      - name: Upload to Firebase App Distribution
        if: steps.fb_ios.outputs.ready == 'true'
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_IOS_APP_ID }}
          token: ${{ secrets.FIREBASE_TOKEN }}
          groups: testers
          file: ${{ env.ADHOC_IPA }}
          releaseNotes: |
            Branch: ${{ github.ref_name }} Commit: ${{ github.sha }}

      - name: Cleanup keychain
        if: always()
        run: security delete-keychain ${{ env.KEYCHAIN_PATH }} 2>/dev/null || true
